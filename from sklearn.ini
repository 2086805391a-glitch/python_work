from sklearn.datasets import make_blobs, load_iris

# 1. 手动生成理想的线性可分数据
X, y = make_blobs(n_samples=100, centers=2, n_features=2, random_state=42)
# 注意：感知机通常要求标签为 1 和 -1
y = np.where(y == 0, -1, 1)

# 2. 加载经典的鸢尾花数据
iris = load_iris()
X_iris = iris.data[:100, :2] # 取前100个样本（两类）和前两个特征
y_iris = np.where(iris.target[:100] == 0, -1, 1)